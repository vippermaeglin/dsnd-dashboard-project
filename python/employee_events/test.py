from employee import Employee
from team import Team

# Make sure your sql queries run without error.
e = Employee()
e.names()
e.username(1)
e.model_data(1)
e.event_counts(1)
e.notes(1)

t = Team()
t.names()
t.username(1)
t.model_data(1)
t.notes(1)